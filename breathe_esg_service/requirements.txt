Django==4.2.13
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
python-decouple==3.8
psycopg2-binary==2.9.9
requests==2.31.0
Pillow==10.3.0
# Phase 2 additions — no new runtime deps required.
# date-fns / date-fns-tz are frontend (npm) packages, not Python.
# Celery is NOT in use; SAP ingestion is synchronous (matches existing pattern).
